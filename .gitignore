# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example

# Per-deployment state (see scripts/up.sh) — the backend domain this
# dashboard was pointed at, persisted so it's only asked once.
.deploy.state
